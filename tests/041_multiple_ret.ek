function returnMultiple(a, b, c)
{
    return (b, c);
}

var (x, y) = returnMultiple(1, 2, 3);
//assert((x == 2) and (y == 3), "returning multiple values from a function is broken");
assert(x == 2 and y == 3, "returning multiple values from a function is broken");
